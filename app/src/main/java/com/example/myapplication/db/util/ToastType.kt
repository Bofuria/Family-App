package com.example.myapplication.db.util

import com.example.myapplication.R

enum class ToastType(val color: Int) {
    SUCCESS(R.color.success_color),
    ERROR(R.color.error_color),
    INFO(R.color.info_color)
}